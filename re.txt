> Role:* You are a Staff Software Engineer and System Architect specializing in *Python (FastAPI)* and *React (TypeScript)*.
> *Project:* A high-performance "Placement Experience Platform" for university students.
> *Technical Stack:*
>  * *Backend:* FastAPI, PostgreSQL, SQLAlchemy (with Alembic for migrations), Pydantic v2.
>  * *Frontend:* React (Vite), TypeScript, Tailwind CSS, TanStack Query (React Query) for state sync.
>  * *Auth:* JWT-based stateless authentication with secure HTTP-only cookies.
> *Core Architecture Requirements:*
>  1. *Schema Design:* >    * Implement a User model with secure password hashing (Argon2/bcrypt).
>    * Implement an Experience model with full CRUD support.
>    * Implement a *Self-Referential Comment model* to support "Instagram-style" nested threading (Parent-Child relationship).
>  2. *Audit Trails:* >    * Every post and comment must track created_at and updated_at.
>    * Logic: If updated_at > created_at, the UI must display an "(edited)" label.
>  3. *The "Instagram Flow" Interaction:*
>    * Build a recursive React component for comments that supports infinite nesting levels visually.
>    * Use *Optimistic UI Updates* via TanStack Query so that liking or commenting feels instantaneous to the user.
>  4. *Performance & UX:*
>    * Implement a "Theme Provider" for seamless Light/Dark mode switching.
>    * Ensure the UI is "Smooth and Clean"—use skeleton loaders for data fetching states.
>    * Backend must include *Pagination (Cursor-based)* for the experience feed to handle thousands of entries without "hanging."
> *Security & Engineering Standards:*
>  * Use *Dependency Injection* in FastAPI for database sessions.
>  * Implement *CORS middleware* correctly for the React/Python split.
>  * Provide a comprehensive docker-compose.yml that orchestrates the FastAPI app, the React frontend, and a PostgreSQL container.
> *Deliverable:*
> Provide the *ER Diagram logic, the **SQLAlchemy models, the **FastAPI endpoint for nested comments, and the **Recursive React Comment component*. Start with a high-level system design overview.
>